# 操作系统笔记

## 不做太细的文档，已经开强化了，注重容易忘记的和一些比较冷的知识点 

#### @author:ChenXF

```
1.函数调用硬件自动保存只有pc，push压栈

2.中断处理的软硬操作
```
![alt text](/Images/中断的软硬操作.png)
```
3.系统调用表本质上是函数指针数组
```
![alt text](/Images/系统调用.png)
```
4.操作系统引导  和   完整流程
```
![alt text](/Images/操作系统引导.png)
![alt text](/Images/计算机启动完整流程.png)
```
5.虚拟机
```
![alt text](/Images/虚拟机.png)
```
6.PCB内容
```
![alt text](/Images/PCB内容.png)
![alt text](/Images/PCB内容1.png)
```
7.引入线程后可线程间切换也可进程间切换

8.线程是轻量级实体，同一进程内线程共享进程地址空间与大部分资源，线程的创建、切换、销毁时空开销远小于进程；引入线程降低系统时空开销

9.微内核的基本功能（与硬件紧密相关的）：进程线程管理(通信、切换、调度、多处理机之间的同步)、低级存储器管理(最低级的，用于逻辑地址->物理地址等的页表机制和地址转换机制)、中断和陷入处理、低级I/O

10.open系统调用的参数需要包含文件的路径名和文件名，而read只需要使用open返回的文件描述符

11.时间片轮转调度（RR）属于抢占式调度

12.管道不是普通磁盘文件，数据存在内核内存，磁盘不存传输内容；
无名管道不能用于无关进程，FIFO 可以；
管道数据读取一次后就消失，不能重复消费；
单向通信只需一根管道，双向必须两根。

13.几种不适合处理机调度的情况：（处于临界区时可以）
  1.在处理中断的过程中
  2.进程再操作系统内核程序临界区中
  3.其他需要完全屏蔽中断的原子操作过程中

14.“中断”是让操作系统内核夺回CPU的唯一途径

15. 父子进程关系
```
![alt text](/Images/父子进程资源关系.png)
```
16.僵尸进程和孤儿进程
```
![alt text](/Images/僵尸孤儿.png)
```
17.进程间通信
```
![alt text](/Images/进程间通信.png)
```
18.线程的优点；便于进程通信，节约系统资源、提高系统并发性

19.RR时间片轮转算周转时间时记得进程到的先后次序

20.装入时动态链接优点：是便于修改和更新，便于实现对目标模块的共享

21.中断处理函数、信号处理函数、多线程共用函数、递归函数，必须按可重入规范编写。

22.PV操作是低级进程通信原语

23.管程通过编译器实现各进程互斥，程序员不同关心，通过在管程内设置条件变量和等待/唤醒操作解决同步问题

24.请求分页地址转换
```
![alt text](/Images/请求分页地址转换.png)
```
25.同一进程内所有线程共享同一份页表基址

26.操作系统实现分区存储管理的代价最小

27.引入段式存储主要满足用户如下需求：
  用户程序由多个逻辑独立模块构成，分段机制匹配程序逻辑结构；
  各逻辑段可独立扩容，解决单一连续空间无法单独扩展模块的问题；
  便于实现代码段、数据段的内存共享与访问权限保护；
  支持模块动态链接、动态加载，提升内存利用率。

28.静态动态重定位
```
![alt text](/Images/静动态重定位.png)
```
29.虚拟存储技术是补充内存逻辑地址

30.基本分页和请求分页
```
![alt text](/Images/基本分页和请求分页.png)
```
31.导致LRU算法实现起来耗费高的原因是需要对所有的页进行排序，进而导致我们去选择硬件的支持

32.工作集不一定是驻留集的子集

33.页面换进换出效率的影响因素三个---->两个频率一个算法

34.虚拟地址空间的大小由底层的虚拟内存管理机制和操作系统决定，而内存和硬盘的大小仅决定虚拟存储器实际可用容量的最大值

35.数据块存文件原始数据，适合大块减少顺序 IO；索引块仅存地址条目，适合小块加快随机检索；

36.操作系统pv中，1进程和3进程互斥访问c，2进程与3进程互斥访问c，但是1和2不互斥访问c，如果三个只设置一个互斥信号量的话会影响整个的并发度，咋可以1 3之间一个，2 3之间一个

37.哲学家进餐中为了实现最多个哲学家同时进餐，可通过外界设置来限制哲学家只能有n - 1个作为信号量,像碗这种

38.inode 本质是去除文件名字段后的 FCB；传统 FCB 将文件名与文件描述信息合一存放，UNIX 把二者分离，文件名放目录项，其余信息存入 inode。

39.内存索引结点多了索引节点号(用于标识内存索引)、状态(上锁或修改)、访问计数、逻辑设备号(inode 所属文件系统的逻辑设备号)、链接指针

40.系统全局打开文件表（所有进程共享，struct file）
每条表项代表一次打开文件实例，完整字段：
  文件读写偏移量 offset（每个打开实例独立，核心考点）
  文件状态打开标志 f_flags：O_RDONLY/O_WRONLY/O_RDWR/O_APPEND/O_TRUNC 等
  引用计数 count：多少个进程 fd 指向本表项，计数为 0 才释放
  指向 inode/vnode 的指针：找到磁盘文件静态信息
  文件操作函数指针 f_op：read/write/lseek/close，区分普通文件 / 设备 / 管道 /socket
  文件所属逻辑设备号 i_dev 缓存（辅助定位磁盘分区）
  文件在磁盘上的位置、访问日期、文件大小

41.多路选择器的运用
```
![alt text](/Images/多路选择器运用场景.png)
```
42.两类虚拟机的差别
```
![alt text](/Images/两类虚拟机差别.png)
```
43.操作系统的功能未必都在内核中，比如图形化用户界面(GUI)

44.超级快的作用：
  1)全局定位：记录 inode 区、数据块区在磁盘上的起始位置，操作系统才能找到 inode 和文件数据；
  2)资源分配管理：维护空闲 inode、空闲数据块的统计信息，创建 / 删除文件时分配回收磁盘资源；
  3)文件系统整体管控：保存分区规格、挂载状态、校验信息，开机时依靠超级块识别、加载文件系统。

45.“IO 控制方式完全由硬件决定” → 错误；“硬件决定设备能够使用的 IO 控制方式” → 正确。

46.共享设备必须是可寻址和可随机访问的设备

47.DMA 预处理代码运行在内核态 ✔
  但执行预处理的过程不需要中断，中断只在传输完成时才产生；
  内核态只是执行权限，和是否产生中断没有必然联系。

48.管程是被进程调度的，管程是语法范围，无法创建和撤销

49.引入 SPOOLing 技术后，用户进程提交打印请求仅需将数据送入磁盘输出井，操作完成即可继续运行，不会因等待物理打印机而阻塞；仅当输出井存储空间耗尽时，进程才会短暂阻塞。

50.用户缓冲（库层面）：减少 write 系统调用次数；
  内核缓冲（OS 层面）：减少真实磁盘写入次数，合并小块写操作。
  read：内核缓冲 → 用户缓冲
  write：用户缓冲 → 内核缓冲

51.磁盘交换区存放两类页面：
   被挂起进程的全部页面；
   内存置换淘汰、但进程未挂起的页面。

52.逻辑存储容量由CPU寻址范围决定

53.目录的检索是靠一个一个比对的

54.用户缓冲区指针：调用 read 时主动传给内核；
   用户能使用缓冲区里的数据：read 系统调用成功返回之后；
   内核缓冲区指针：全程不暴露给用户。

55.看到名词 撤销原语：执行流程包含终止全部子孙进程；
  没有指明撤销原语，只说 “进程终止、正常结束”：子进程不会被终止，产生孤儿进程；
  经典选择题：操作系统终止进程时不一定执行的操作？答案：终止子进程。
  理由：只有撤销原语强制销毁才执行，进程自主 exit 不会执行该步骤。

56.通信方式
```
![alt text](/Images/共享存储.png)
![alt text](/Images/消息传递.png)
![alt text](/Images/管道通信.png)
```
57.当前进程主动放弃处理机
     - 处理机进程正常终⽌
     - 运⾏过程中发⽣异常⽽终⽌
     - 进程主动请求阻塞如等待(I/O)
   处理中断过程中和内核临界区（要内核，内核临界区是访问内核某种数据结构的）不能进行进程调度

58.死锁必然存在循环等待；循环等待是死锁的必要条件，不是充分条件。

59.死锁产生原因（诱因）：竞争资源、推进顺序非法、信号量使用不当

60.内存映射文件
```
![alt text](/Images/内存映射文件.png)
```
61.磁盘坏块的两个方案：
✅ OS 标记坏块（FAT 表）→ 不透明 -- 逻辑初始化
✅ 磁盘控制器 + 备用扇区自动替换 → 透明 -- 物理初始化
```