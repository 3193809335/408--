# 数据结构笔记

## 不做太细的文档，已经开强化了，注重容易忘记的和一些比较冷的知识点 

#### @author:ChenXF

```
1.绝大多数递归转非递归，用栈模拟函数调用栈，是通用标准方法；但存在特例不用栈。--尾递归、单向递推无回溯 → 只用循环变量，不需要栈。

2.所有递归都可以转为非递归
```
```python 
def f(n):                 //斐波那契
    a,b = 1,1
    for _ in range(3,n+1):
        a,b = b,a+b
    return b
```
```
3.孩子表示法 顺序存储+链式存储
```
![alt text](/Images/孩子表示法.png)
```
4.满足前、中、后序遍历序列相同的二叉树，结构只有两种：空二叉树、仅一个根结点的二叉树；不存在第三种结构。

5.具有n个结点的k叉树的最小高度为 --------> logk((k - 1) * n + 1)(向上取整)
                     最大高度为 --------> n - k + 1
              正则树的最大高度为 --------> (n - 1) / k + 1

6.题干要求强制构造 m 叉哈夫曼树（允许补 0 权虚叶）：非叶子结点数量 = ⌈(n-1)/(m-1)⌉。即非叶子结点数为上取整((n-1)/(m-1))。

7.给定任意一种单一遍历序列（仅先序 / 仅中序 / 仅后序），n 个互不相同结点，能构造的不同二叉树数量都是第 n 个卡特兰数：
                                C_n = 1 / (n + 1) * C(2n, n)
先序 + 中序 / 中序 + 后序：唯一一棵树，数量 = 1
先序 + 后序：不能唯一确定，数量不一定，不能用卡特兰

8.要交换其所有分支结点的只有子树的位置，利用中序遍历方法最不适合

9.并查集(目前没考过)可以用来验证 “树的边数 + 1 = 结点树” 这个规律  摊还复杂度O(alpha(n)))，近似常数，远小于\(\log n\)。
```
```c++
  //并查集
  #define SIZE  100
  int UFSets[SIZE];
  void Initial(int S[]){
    for(int i = 0; i < SIZE; i++){
      S[i] = -1;
    }
  }
  //查询操作 
  int Find(int S[], int x){ 
    while(S[x] >= 0){
      x = S[x]; 
    }
    return x;
  }   //普通法(log_2 n)
  int Find(int S[],int x){
    int root = x;
    while(S[root] >= 0){
      root = S[root];
    }
    while(x != root){
      int t = S[x];
      S[x] = root;
      x = t;
    }
    return root;
  } // 路径压缩(log)

  //合并
  void Union(int S[], int Root1, int Root2){
    int rootX = find(Root1);
    int rootY = find(Root2);
    if (rootX != rootY) {
            // 按秩合并
`    if (rank[rootX] < rank[rootY]) {
       S[rootX] = rootY;
      } else if (rank[rootX] > rank[rootY]) {
       S[rootY] = rootX;
     } else {
       S[rootY] = rootX;
       rank[rootX]++; // 高度增加
     }
       count--;
     }
  }
```
```
10.
AOV 网(顶点表示活动: Activity On Vertix):
  2. 核心用途
  拓扑排序，判断工程是否存在环（有环代表活动循环依赖，工程无法完成）。
  无环 AOV：可输出拓扑序列；
  有环 AOV：不存在拓扑序列，工程矛盾。
  3. 特征
  边只表示先后顺序，无权重；
  只关心活动先后逻辑，不关心耗时；
  典型操作：拓扑排序、判环。  

AOE 网（边表示活动，Activity On Edge）:
  1. 定义
  有向带权无环图（DAG）
  有向边代表实际活动，边上权值 = 该活动持续时间；
  顶点代表事件（标志前面活动全部完成、后面活动可以开始）。
  2. 固定两个特殊顶点
  源点：入度为 0，工程起点；
  汇点：出度为 0，工程终点。
  3. 核心用途
  求关键路径，找出影响工程总工期的关键活动，缩短关键活动才能缩短总工期。

11.
```
![alt text](/Images/邻接多重表.png)
![alt text](/Images/十字链表法.png)

```
12.邻接矩阵在空间上和遍历某顶点所有邻接上（操作更麻烦） 比不过邻接表

13.图不可以为空

14.邻接矩阵的BFS、DFS的时间复杂度都是O（n^2）
   邻接表的BFS、DFS的时间复杂度都是O（n + e）

15.算法	     能否有负权边	   能否有负权环
Dijkstra	    ❌ 不行	       ❌ 不行
Floyd	        ✅ 可以	      ❌ 不能存在（但能检测）

16.判断是否存在回路
```
![alt text](/Images/判断存在回路方法.png)
```
17.AOE网中时间余量为：vl(头) - ve(尾) - 活动时间

18.分块查找查找效率优于顺序查找，插入删除比二分有序表方便，结构简单，适合动态线性表。

19.线性探测：一次聚集，冲突区块连成大片
  二次探测：消除一次聚集，但仍存在二次聚集（同哈希地址关键字探测路径相同）
  双哈希无聚集。

20.存储效率 = 实际存放有效数据的空间 ÷ 分配给该结构的总空间，衡量空间有没有被浪费。

21.分块查找索引部分默认还是顺序查找

22.给定一个序列则对应的二叉排序树种类也是通过卡特兰数计算
```
 
 
