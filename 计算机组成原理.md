# 计算机组成原理笔记

## 不做太细的文档，已经开强化了，注重容易忘记的和一些比较冷的知识点 

### 计算机组成原理考得相对操作系统来说比较固定死板，打好基础做好真题就没什么问题


#### @author:ChenXF


## [学习链接](https://www.zhihu.com/column/c_1962152858632839996)


```
1.MAR地址位数决定了主存地址空间大小，而非实际大小 

2.非写分配法通常于全写法合用，写分配法通常和回写法何用

3.回写法适用于访问密集型应用，其中包含写操作

4.cache功能cahce缺失、TLB缺失 全部由硬件实现，cache与主存地址空间相互独立

5.微程序比硬布线更占用空间，占用芯片资源

6.缺页处理只修改主存页表，不修改 TLB；TLB 不会自动同步页表更新。

7.段页式采用页为单位

8.段的分界与程序的逻辑分界相对应，使得它易于编译、修改、保护和共享

9.通常逻辑地址的位数比物理地址的位数多

10.内存管理单元（MMU）完成地址的转换

11.页表只能由操作系统和硬件进行访问（×进程）

12.主存-外存通常采用全相联、替换算法由软件实现、采用回写法

13.中断（广义）是操作系统执行内核的唯一途径

14.指令算出最少最多位数后，题目说按字节编址，要记得是8的整数倍

15.转移指令（控制转移指令）实现程序控制

16.转移目标地址一定在指令中明显给出

17.非嵌套调用时可返回地址可保存在特定寄存器中

18.
```
![alt text](Images/image%20copy.png)

```
19.CISC便于汇编程序员编译，内部通用寄存器比RISC少

20.通用寄存器的位数取决于机器字长

21.控制器可区分存储单元中存放的是指令还是数据

22.数据通路包括数据在指令执行过程中所经过的路径，以及路径上的涉及的硬件部件，还包括中断机制、用于异常事件的检测及响应的电路等

23.硬布线（组合逻辑）CU 的全部输入信号，分四大类
  1. 指令相关输入（来自指令寄存器 IR,经指令译码器产生的）
    指令操作码 OP、寻址方式字段，译码后送入 CU；
    作用：区分当前是哪一条指令，生成对应微操作。
  2. 时序节拍信号（来自时序发生器）
    节拍脉冲 (T1,T2,T3...)、周期状态标记；
    作用：区分当前处于取指 / 译码 / 执行 / 访存 / 写回哪一拍，同一指令不同节拍输出不同控制信号。
  3. 运算状态标志（来自 ALU/PSW 程序状态字）
    零标志 ZF、进位 CF、溢出 OF、符号位 SF 等；
    作用：判断条件分支、跳转是否生效。
  4. 外部 / 系统控制信号
    复位 Reset、中断请求、流水线 stall、cache 命中信号等；
    作用：处理异常、暂停流水线、整机初始化。

24.
```
![alt text](/Images/中断处理软硬件做了什么.png)

```
25.右规和尾数舍入都可能发生上溢

26.在程序正常执行时，通过顺序执行指令过转移指令得到的控制流称为正常控制流，遇到异常或中断事件而引起公户出现的正常执行被打断所形成的意外控制流，称为异常控制流

27.“故障”类异常的断点为当前发生异常的指令的地址
“终止”类异常可以是当前指令或下一条指令的地址

28.流水线CPU是时间并行性，多处理器是空间并行性

29.流水线数据通路的取指令阶段和指令译码阶段不需要控制信号的控制

30.数据通路不包括控制部件，由组合逻辑电路和时序控制部件组成

31.MEM 阶段同时会更新 PC 寄存器（控制通路，和访存无关）
第 5 周期：才会用新 PC 执行 IF，取出分支后的指令
所以控制冒险的下一条指令在该指令的WB阶段开始IF

32.load-use类型的数据会引起一个或多个时钟周期的阻塞

33.CPU每条指令执行完毕，硬件自动检查 INTR 引脚（查看 8259 是否发了中断请求）；1
```
![alt text](/Images/中断响应和处理.png)

```
34.SMP共享存储器，各CPU仍可在各自独立的虚拟地址空间中运行程序
SMP根据内存访问延迟的特性分为：
  统一存储访问UMA多处理器
  非统一存储访问NMUA多处理器
```
![alt text](/Images/SMP和CMP.png)

```
35.超线程技术的CPU需要芯片组、操作系统和应用软件的支持，才能发挥该项技术的优势

36.标量流水线处理机是SISD，并行处理机==阵列处理机==向量处理机--SIMD

37.NUMA理论上可以无线扩展，而UMA会加大访存冲突

38.多核处理器是在一颗CPU上集成了多个执行内核而非控制核心的处理器

39.
```

![alt text](/Images/总线周期.png)

```
40.中断过程
```
![alt text](/Images/中断过程.png)
![alt text](/Images/中断过程2.png)

```
41.形成中断服务程序的入口地址是中断隐指令完成的

42.cpu检测不到处理优先级更低的中断请求信号

43.DMA传送前由CPU通过系统调用执行设备驱动程序设置传送参数

44.CPU响应DMA请求的条件是当前机器周期执行完

45.中断屏蔽字可以改变多个中断服务程序执行完的次序

46.表示范围：浮点 > 定点整型
精度（有效数字位数）：同二进制位数下，浮点 < 整型

47.传送一个 ASCII 字符，帧里必须包含 1 位起始位

48.在中断大题中,问到完成一个传送1000个字符花费的时钟周期 和 CPU用于完成这一任务时间周期是不同的   前面那个要关注设备准备的时间和CPU是并行的
例子：
```
![alt text](/Images/中断例子1.png)
```
49.无流水线寄存器情况下是采用非流水线形式，即不采用五级划分，则该执行阶段包括了写回

50.8位字长的计算机，int通常是8位   16-16   32-32  64-32

51.一条指令的缺页前后会访问TLB两次

52.三态门：用于控制xxxx与总线的连接和断开

53.算术左移时，低位补0（包括符号位左移）；算术右移时，高位补符号位

54.机器级语言包括汇编语言和机器语言

55.机器字长越大，数的表示范围越大，计算精度越高

56.编译程序产生目标代码执行效率高，但可移植性差;解释程序具有良好的可移植性，但执行效率低

57.ALU中通过采用锁存器传送操作数，通常比使用多路选择器慢，因为锁存器需要专门的数据路径和控制逻辑来选择和传递操作数

58.阶码部件只能进行阶码相加、相减和比较操作

59.使用带查找表的并行加法器实现时，可以在一个时钟周期内完成加法运算

60.阵列乘法器可认为全并行运算的乘法器

61.加法器的输入信息负数那个输入信息记得是取反就行 不要再+1

62.short/unsgined short ->  unsigned int 都是前面补0

63.写分配法是把主存块调入cache，再对cache写

64.MDR位数只和存储器总线的宽度有关

65.补码和原码除法的加减交替法区别
```
![alt text](/images/原码补码除法.png)
```
66.求引脚数时要包括行选通和列选通线各一根，读写控制线各一根，且多模块单个来看的地址线只和该芯片有关

67.cache-主存的效率为： 访问cache的t / 平均访问时间t

68.主存地址除标记位 其余为cache的地址

69.主存-外存 为了提高命中率通常采用全相联映射和管理

70.TLB中多个访问位

71.指令系统是程序员可见的实际指令的集合，由计算机的体系结构决定，而非计算机组成

72.相同计算任务，Load/Store 指令集的程序长度 > 通用寄存器（RM）风格程序长度。

73.定长指令相比变长指令不一定产生更多的空间浪费

74.由于CISC指令可以执行多个操作，所以CISC的编码更长,存储指令占用的空间更大。RISC的寻址方式更少，CISC比RISC机器可以更好的支持高级语言

75.控制器主要由pc、IR、指令译码器、MAR、MDR以及时序电路等组成

76.指令周期时长不固定

77.不是所有指令的取指操作都是相同的

78.水平型微指令指令字的一位对应一个控制信号，控制器需要较长的控制域-微指令控制字段长度巨大。

79.分支预测技术可通过硬件（运行时动态检测）和软件（编译时动态优化）共同实现，属于软硬协同的典型技术

80.指令流水线中，各阶段执行时间可不同，流水线 的时钟周期由最长阶段的执行时间决定（其他阶段会等待）

81.多周期CPU硬件结构比单周期复杂，因为需要流水线，控制逻辑复杂

82.单周期计算机的数据和指令可以存在一个存储器中(双端口下)

83.控制部件不属于数据通路，包含取值部件

84.指令译码器产生的控制信号，通过流水段寄存器的传递，与对应的指令数据同步流转，流水段寄存器的位数不一样

85.寄存器重命名（体系结构 / 计组高频考点）
消除 名字相关（假相关）：WAR（写后读）、WAW（写后写）
⚠️重点：无法消除 RAW（写后读，真数据相关）

86.Cache、TLB缺失、访存冲突会产生指令流水线阻塞

87.非条件转移指令（无条件转移 jmp）依然会产生控制相关！

88.多线程程序下，双核双线程处理器上运行速度是单核单线程的2倍
```
![alt text](/Images/粗细多线程.png)
![alt text](/Images/UMA-NUMA.png)
![alt text](/Images/核心技术汇总.png)
```
89.数据/地址复用并不是没有数据线，也不是单纯地只通过地址线传输数据
```